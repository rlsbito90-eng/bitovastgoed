name: Temp patch acquisitie dossierrij selectors

on:
  push:
    branches:
      - fix/acquisitie-opvolgen-compact-2

permissions:
  contents: write

jobs:
  patch:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/acquisitie-opvolgen-compact-2
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Pas gerichte patch toe
        run: python scripts/tmp-patch-acquisitie-dossierrij-selectors.py
      - name: Verwijder tijdelijke patch-infrastructuur
        run: |
          git rm scripts/tmp-patch-acquisitie-dossierrij-selectors.py
          git rm .github/workflows/tmp-patch-acquisitie-dossierrij-selectors.yml
      - name: Commit en push
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/components/offmarket/acquisitie/AcquisitieDossierRij.tsx
          git commit -m "BUILD acquisitie: maak opvolgkaart selectors expliciet"
          git push origin HEAD:fix/acquisitie-opvolgen-compact-2
