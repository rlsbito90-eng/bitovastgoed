name: Apply bid flow follow-up

on:
  push:
    branches:
      - feat/bid-flow-auto-progress
    paths:
      - scripts/codemod_bid_flow_followup.py

permissions:
  contents: write

jobs:
  codemod:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/bid-flow-auto-progress
          fetch-depth: 0
      - run: python scripts/codemod_bid_flow_followup.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          git diff --cached --quiet && { echo "No changes to commit"; exit 1; }
          git commit -m "CRM: harden biedingsprojectie en kandidaatweergave"
          git push origin HEAD:feat/bid-flow-auto-progress
