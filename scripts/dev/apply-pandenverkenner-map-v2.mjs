import fs from 'node:fs';

function replace(path, from, to) {
  let s = fs.readFileSync(path, 'utf8');
  if (!s.includes(from)) throw new Error(`Anchor ontbreekt in ${path}: ${from.slice(0,100)}`);
  s = s.replace(from, to);
  fs.writeFileSync(path, s);
}
function appendBefore(path, marker, addition) {
  let s = fs.readFileSync(path, 'utf8');
  if (!s.includes(marker)) throw new Error(`Marker ontbreekt in ${path}`);
  s = s.replace(marker, `${addition}\n\n${marker}`);
  fs.writeFileSync(path, s);
}

// queryService: V3 request + validator
replace('src/lib/bag/queryService.ts',
`export type BagKaartAanvraagV2 = Omit<BagPandZoekAanvraagV4, 'naIdentificatie' | 'limiet'> & {\n  viewport: BagWgs84Viewport;\n  limiet: number;\n};`,
`export type BagKaartAanvraagV2 = Omit<BagPandZoekAanvraagV4, 'naIdentificatie' | 'limiet'> & {\n  viewport: BagWgs84Viewport;\n  limiet: number;\n};\n\nexport type BagKaartAanvraagV3 = BagKaartAanvraagV2 & {\n  zoom: number;\n};`);
appendBefore('src/lib/bag/queryService.ts', 'export function valideerPandZoekAanvraag(',
`export function valideerKaartAanvraagV3(aanvraag: BagKaartAanvraagV3): BagQueryValidatie {\n  const fouten = [...valideerKaartAanvraagV2(aanvraag).fouten];\n  if (!Number.isFinite(aanvraag.zoom) || aanvraag.zoom < 7 || aanvraag.zoom > 22) {\n    fouten.push('De kaartzoom moet tussen 7 en 22 liggen.');\n  }\n  return { geldig: fouten.length === 0, fouten };\n}`);

// queryTransport: import + V3 action
replace('src/lib/bag/queryTransport.ts', '  valideerKaartAanvraagV2,', '  valideerKaartAanvraagV2,\n  valideerKaartAanvraagV3,');
replace('src/lib/bag/queryTransport.ts', '  type BagKaartAanvraagV2,', '  type BagKaartAanvraagV2,\n  type BagKaartAanvraagV3,');
appendBefore('src/lib/bag/queryTransport.ts', 'export async function haalCbsGebiedsopties',
`export async function haalPandenOpKaartV3<T>(aanvraag: BagKaartAanvraagV3): Promise<BagTransportResultaat<T>> {\n  const validatie = valideerKaartAanvraagV3(aanvraag);\n  if (!validatie.geldig) throw new TypeError(validatie.fouten.join(' '));\n  controleerScope(aanvraag.scopeCode);\n  return invoke<T>({\n    action: 'viewport_v3', scopeCode: aanvraag.scopeCode,\n    minLon: aanvraag.viewport.minLon, minLat: aanvraag.viewport.minLat, maxLon: aanvraag.viewport.maxLon, maxLat: aanvraag.viewport.maxLat,\n    zoom: aanvraag.zoom, limit: aanvraag.limiet,\n    bouwjaarVan: aanvraag.bouwjaarVan, bouwjaarTot: aanvraag.bouwjaarTot, statussen: aanvraag.statussen,\n    vboOppervlakteSomVan: aanvraag.vboOppervlakteSomVan, vboOppervlakteSomTot: aanvraag.vboOppervlakteSomTot,\n    vboOppervlakteMaxVan: aanvraag.vboOppervlakteMaxVan, vboOppervlakteMaxTot: aanvraag.vboOppervlakteMaxTot,\n    vboAantalVan: aanvraag.vboAantalVan, vboAantalTot: aanvraag.vboAantalTot, gebruiksdoelen: aanvraag.gebruiksdoelen,\n    isGemengd: aanvraag.isGemengd, vboModus: aanvraag.vboModus, wijkCodes: aanvraag.wijkCodes, buurtCodes: aanvraag.buurtCodes,\n  }, { retryBijNetwerkfout: true });\n}`);

// Edge: add viewport_v3 before viewport_v2, reusing same validation boundaries.
const edgePath = 'supabase/functions/bag-query-service/index.ts';
let edge = fs.readFileSync(edgePath, 'utf8');
const viewportV2Marker = "  if (body.action === 'viewport_v2') {";
if (!edge.includes(viewportV2Marker)) throw new Error('viewport_v2 marker ontbreekt');
const v3block = `  if (body.action === 'viewport_v3') {\n    const scope = scopeCode(body.scopeCode);\n    const minLon = coordinate(body.minLon); const minLat = coordinate(body.minLat); const maxLon = coordinate(body.maxLon); const maxLat = coordinate(body.maxLat);\n    const zoom = optionalNumber(body.zoom, 7, 22, 'Kaartzoom');\n    if (zoom === null) throw new TypeError('Kaartzoom ontbreekt');\n    const limit = integer(body.limit ?? 1500, 1, 1500, 'Kaartlimiet');\n    if (minLon < 3.0 || maxLon > 8.0 || minLat < 50.0 || maxLat > 54.5 || minLon >= maxLon || minLat >= maxLat) throw new TypeError('Kaartviewport valt buiten de begrensde Nederlandse WGS84-zone');\n    const bouwjaarVan = optionalInteger(body.bouwjaarVan, 1000, 3000, 'Bouwjaar vanaf'); const bouwjaarTot = optionalInteger(body.bouwjaarTot, 1000, 3000, 'Bouwjaar tot');\n    const statussen = textArray(body.statussen, 'pandstatusselectie'); const wijkCodes = textArray(body.wijkCodes, 'wijkselectie', 64); const buurtCodes = textArray(body.buurtCodes, 'buurtselectie', 128);\n    if (wijkCodes.some(code => !/^WK[0-9]{4}[A-Z0-9]{2}$/.test(code) || code.slice(2, 6) !== scope)) throw new TypeError('Ongeldige wijkselectie');\n    if (buurtCodes.some(code => !/^BU[0-9]{4}[A-Z0-9]{4}$/.test(code) || code.slice(2, 6) !== scope)) throw new TypeError('Ongeldige buurtselectie');\n    const vboSomVan = optionalNumber(body.vboOppervlakteSomVan, 0, 100_000_000, 'VBO-oppervlakte som vanaf'); const vboSomTot = optionalNumber(body.vboOppervlakteSomTot, 0, 100_000_000, 'VBO-oppervlakte som tot');\n    const vboMaxVan = optionalNumber(body.vboOppervlakteMaxVan, 0, 10_000_000, 'VBO-oppervlakte max vanaf'); const vboMaxTot = optionalNumber(body.vboOppervlakteMaxTot, 0, 10_000_000, 'VBO-oppervlakte max tot');\n    const vboAantalVan = optionalInteger(body.vboAantalVan, 0, 100_000, 'VBO-aantal vanaf'); const vboAantalTot = optionalInteger(body.vboAantalTot, 0, 100_000, 'VBO-aantal tot');\n    const gebruiksdoelen = textArray(body.gebruiksdoelen, 'gebruiksfunctieselectie'); const isGemengd = optionalBoolean(body.isGemengd, 'isGemengd');\n    const vboModus = body.vboModus == null ? 'alle' : String(body.vboModus).trim(); if (!VBO_MODI.has(vboModus)) throw new TypeError('Ongeldige VBO-modus');\n    validateRanges(bouwjaarVan, bouwjaarTot, vboSomVan, vboSomTot, vboMaxVan, vboMaxTot, vboAantalVan, vboAantalTot);\n    return sql.begin(async (tx) => {\n      await tx.unsafe('SET LOCAL ROLE bag_reader');\n      return tx\`SELECT * FROM bag_service.panden_kaart_v3(\${scope}, \${minLon}, \${minLat}, \${maxLon}, \${maxLat}, \${zoom}, \${limit}, \${bouwjaarVan}, \${bouwjaarTot}, \${statussen}, \${vboSomVan}, \${vboSomTot}, \${vboMaxVan}, \${vboMaxTot}, \${vboAantalVan}, \${vboAantalTot}, \${gebruiksdoelen}, \${isGemengd}, \${vboModus}, \${wijkCodes}, \${buurtCodes})\`;\n    });\n  }\n`;
edge = edge.replace(viewportV2Marker, v3block + viewportV2Marker);
fs.writeFileSync(edgePath, edge);

// kaartModel: V3 rows + feature builders while preserving V2 exports.
appendBefore('src/lib/bag/kaartModel.ts', 'function getal(',
`export interface BagKaartV3Rij {\n  item_type: 'cluster' | 'pand'; cluster_id: string | null; aantal: number; datasetversie_id: number | null; index_build_id: number | null; identificatie: string | null;\n  status: string | null; bouwjaar: number | null; vbo_aantal: number | null; vbo_oppervlakte_som: number | string | null; gebruiksdoelen: string[] | null; is_gemengd: boolean | null;\n  primair_adres: string | null; primair_postcode: string | null; primair_plaats: string | null; wijk_code: string | null; wijk_naam: string | null; buurt_code: string | null; buurt_naam: string | null;\n  center_geojson: Point | null; pand_geojson: import('geojson').Polygon | import('geojson').MultiPolygon | null; afgekapt: boolean;\n}\n\nexport interface BagKaartV3FeatureProperties extends BagKaartFeatureProperties { itemType: 'cluster' | 'pand'; aantal: number; clusterId: string; }`);
appendBefore('src/lib/bag/kaartModel.ts', 'export function isBagKaartAfgekapt',
`export function bouwBagKaartV3Punten(rows: BagKaartV3Rij[]): FeatureCollection<Point, BagKaartV3FeatureProperties> {\n  return { type: 'FeatureCollection', features: rows.filter(row => geldigPunt(row.center_geojson)).map(row => ({\n    type: 'Feature', geometry: row.center_geojson as Point, properties: {\n      itemType: row.item_type, aantal: Number(row.aantal) || 0, clusterId: row.cluster_id ?? '', id: row.identificatie ?? row.cluster_id ?? '',\n      adres: row.primair_adres ?? (row.item_type === 'cluster' ? \`\${Number(row.aantal) || 0} panden\` : \`BAG-pand \${row.identificatie ?? ''}\`),\n      postcode: row.primair_postcode ?? '', plaats: row.primair_plaats ?? '', status: row.status ?? '', bouwjaar: Number.isFinite(row.bouwjaar) ? row.bouwjaar : null,\n      gbo: getal(row.vbo_oppervlakte_som), vboAantal: Number.isFinite(row.vbo_aantal) ? row.vbo_aantal : null, wijk: row.wijk_naam ?? '', buurt: row.buurt_naam ?? '',\n    }\n  })) };\n}\n\nexport function bouwBagKaartV3Contouren(rows: BagKaartV3Rij[]): import('geojson').FeatureCollection<import('geojson').Polygon | import('geojson').MultiPolygon, { id: string }> {\n  return { type: 'FeatureCollection', features: rows.filter(row => row.item_type === 'pand' && row.identificatie && row.pand_geojson).map(row => ({ type: 'Feature', geometry: row.pand_geojson!, properties: { id: row.identificatie! } })) };\n}`);

// kaartSession v2 row type/version
replace('src/lib/bag/kaartSession.ts', "import type { BagKaartPandRij } from './kaartModel';", "import type { BagKaartV3Rij } from './kaartModel';");
replace('src/lib/bag/kaartSession.ts', '  versie: 1;', '  versie: 2;');
replace('src/lib/bag/kaartSession.ts', '  rows: BagKaartPandRij[];', '  rows: BagKaartV3Rij[];');
replace('src/lib/bag/kaartSession.ts', 'bito:bag:pandenverkenner:kaart:v1:', 'bito:bag:pandenverkenner:kaart:v2:');
replace('src/lib/bag/kaartSession.ts', 'sessie.versie === 1', 'sessie.versie === 2');
replace('src/lib/bag/kaartSession.ts', '...input, versie: 1', '...input, versie: 2');

// Replace map component fully with server-cluster V3 implementation.
const mapPath = 'src/components/bag/BagPandenKaart.tsx';
fs.writeFileSync(mapPath, `import { useCallback, useEffect, useMemo, useRef, useState } from 'react';\nimport Map, { Layer, NavigationControl, Popup, Source, type MapLayerMouseEvent, type MapRef } from 'react-map-gl/maplibre';\nimport maplibregl from 'maplibre-gl';\nimport 'maplibre-gl/dist/maplibre-gl.css';\nimport type { Point } from 'geojson';\nimport { Check, Copy, ExternalLink, Loader2, MapPinned, RefreshCw } from 'lucide-react';\nimport { toast } from 'sonner';\nimport { Badge } from '@/components/ui/badge';\nimport { Button } from '@/components/ui/button';\nimport { bouwGoogleMapsAdresUrl } from '@/lib/bag/googleMaps';\nimport type { BagVerkennerPand } from '@/lib/bag/pandenverkennerModel';\nimport { haalPandenOpKaartV3 } from '@/lib/bag/queryTransport';\nimport { bouwBagKaartV3Contouren, bouwBagKaartV3Punten, type BagKaartFilters, type BagKaartV3FeatureProperties, type BagKaartV3Rij } from '@/lib/bag/kaartModel';\nimport { bewaarKaartSessie, leesKaartSessie } from '@/lib/bag/kaartSession';\n\nconst PDOK_TILE='https://service.pdok.nl/brt/achtergrondkaart/wmts/v2_0/standaard/EPSG:3857/{z}/{x}/{y}.png';\nconst STYLE={version:8 as const,sources:{'pdok-brt':{type:'raster' as const,tiles:[PDOK_TILE],tileSize:256,attribution:'&copy; PDOK / Kadaster',maxzoom:19}},layers:[{id:'pdok-brt',type:'raster' as const,source:'pdok-brt'}],glyphs:'https://demotiles.maplibre.org/font/{fontstack}/{range}.pbf'};\nconst KAART_LIMIET=1500; const AMSTERDAM_VIEWPORT={longitude:4.9041,latitude:52.3676,zoom:10.8};\nconst CLUSTER_LAYER={id:'bag-server-clusters',type:'circle' as const,source:'bag-kaart-v3',filter:['==',['get','itemType'],'cluster'],paint:{'circle-color':'#0f526f','circle-radius':['step',['get','aantal'],18,100,23,500,29,2000,34],'circle-stroke-color':'#fff','circle-stroke-width':1.5}};\nconst CLUSTER_TELLER={id:'bag-server-cluster-teller',type:'symbol' as const,source:'bag-kaart-v3',filter:['==',['get','itemType'],'cluster'],layout:{'text-field':['get','aantal'],'text-size':11},paint:{'text-color':'#fff'}};\nconst PAND_LAYER={id:'bag-v3-panden',type:'circle' as const,source:'bag-kaart-v3',filter:['==',['get','itemType'],'pand'],paint:{'circle-color':'#0f526f','circle-radius':['interpolate',['linear'],['zoom'],15,5,17,8,19,10],'circle-stroke-color':'#fff','circle-stroke-width':1,'circle-opacity':0.92}};\nconst CONTOUR_FILL={id:'bag-pandcontour-fill',type:'fill' as const,source:'bag-pandcontouren',minzoom:17,paint:{'fill-color':'#0f526f','fill-opacity':0.14}};\nconst CONTOUR_LINE={id:'bag-pandcontour-line',type:'line' as const,source:'bag-pandcontouren',minzoom:17,paint:{'line-color':'#0f526f','line-width':2}};\n\ninterface Props { scopeCode:string; filters:BagKaartFilters; geselecteerdeIds?:Set<string>; onKandidaatToggle?:(pand:BagVerkennerPand)=>void; }\nfunction kaartRijNaarVerkennerPand(row:BagKaartV3Rij):BagVerkennerPand { const adres=row.primair_adres?.trim()||row.identificatie||''; return {datasetversieId:String(row.datasetversie_id??''),bagPandId:row.identificatie||'',voorkomenSleutel:'',status:row.status,adres,adresCompleet:Boolean(row.primair_adres?.trim()),straat:null,postcode:row.primair_postcode?.trim()||null,plaats:row.primair_plaats?.trim()||null,wijkCode:row.wijk_code?.trim()||null,wijkNaam:row.wijk_naam?.trim()||null,buurtCode:row.buurt_code?.trim()||null,buurtNaam:row.buurt_naam?.trim()||null,bouwjaar:Number.isFinite(row.bouwjaar)?row.bouwjaar:null,gebruiksdoelen:row.gebruiksdoelen??[],oppervlakte:row.vbo_oppervlakte_som==null?null:Number(row.vbo_oppervlakte_som),aantalVerblijfsobjecten:Number.isFinite(row.vbo_aantal)?Number(row.vbo_aantal):0,gemengdGebruik:Boolean(row.is_gemengd),cursor:''}; }\nfunction formatGetal(value:number|null,suffix=''){return value===null||!Number.isFinite(value)?null:\`\${Math.round(value).toLocaleString('nl-NL')}\${suffix}\`;}\n\nexport default function BagPandenKaart({scopeCode,filters,geselecteerdeIds=new Set(),onKandidaatToggle}:Props){\n const mapRef=useRef<MapRef|null>(null); const focusBewegingRef=useRef(false); const filterKey=useMemo(()=>JSON.stringify(filters),[filters]); const initiëleSessie=useRef(leesKaartSessie(scopeCode,filterKey)).current;\n const [rows,setRows]=useState<BagKaartV3Rij[]>(initiëleSessie?.rows??[]); const [laden,setLaden]=useState(false); const [heeftGezocht,setHeeftGezocht]=useState(initiëleSessie?.heeftGezocht??false); const [kaartVerouderd,setKaartVerouderd]=useState(false); const [geselecteerd,setGeselecteerd]=useState<BagKaartV3FeatureProperties|null>(null);\n useEffect(()=>{const sessie=leesKaartSessie(scopeCode,filterKey);setRows(sessie?.rows??[]);setHeeftGezocht(sessie?.heeftGezocht??false);setKaartVerouderd(false);setGeselecteerd(null);if(sessie?.viewState)mapRef.current?.jumpTo(sessie.viewState);},[scopeCode,filterKey]);\n const punten=useMemo(()=>bouwBagKaartV3Punten(rows),[rows]); const contouren=useMemo(()=>bouwBagKaartV3Contouren(rows),[rows]); const totaalMatches=useMemo(()=>rows.reduce((som,row)=>som+(Number(row.aantal)||0),0),[rows]); const serverClusterMode=rows.some(row=>row.item_type==='cluster'); const afgekapt=!serverClusterMode&&rows.some(row=>row.afgekapt);\n const zoekInKaartgebied=useCallback(async()=>{const map=mapRef.current?.getMap();if(!map)return;const b=map.getBounds();setLaden(true);try{const resultaat=await haalPandenOpKaartV3<BagKaartV3Rij>({scopeCode,viewport:{minLon:b.getWest(),minLat:b.getSouth(),maxLon:b.getEast(),maxLat:b.getNorth()},zoom:map.getZoom(),limiet:KAART_LIMIET,...filters});setRows(resultaat.rows);setHeeftGezocht(true);const c=map.getCenter();bewaarKaartSessie({scopeCode,filterKey,rows:resultaat.rows,heeftGezocht:true,viewState:{longitude:c.lng,latitude:c.lat,zoom:map.getZoom()}});setKaartVerouderd(false);setGeselecteerd(null);if(!resultaat.rows.length)toast.info('Geen BAG-panden in dit kaartgebied voor de gekozen filters.');}catch(error){toast.error(error instanceof Error?error.message:'BAG-kaart laden mislukt.');}finally{setLaden(false);}},[filterKey,filters,scopeCode]);\n const onClickKaart=useCallback((event:MapLayerMouseEvent)=>{const f=event.features?.[0];if(!f){setGeselecteerd(null);return;}if(f.layer.id==='bag-server-clusters'&&f.geometry.type==='Point'){focusBewegingRef.current=true;mapRef.current?.easeTo({center:(f.geometry as Point).coordinates as [number,number],zoom:Math.min((mapRef.current?.getZoom()??10)+1.8,15.2),duration:420});return;}if(f.layer.id==='bag-v3-panden'){const p=f.properties as unknown as BagKaartV3FeatureProperties;setGeselecteerd({...p,id:String(p.id),adres:String(p.adres??''),postcode:String(p.postcode??''),plaats:String(p.plaats??''),status:String(p.status??''),bouwjaar:p.bouwjaar==null?null:Number(p.bouwjaar),gbo:p.gbo==null?null:Number(p.gbo),vboAantal:p.vboAantal==null?null:Number(p.vboAantal),wijk:String(p.wijk??''),buurt:String(p.buurt??''),itemType:'pand',aantal:1,clusterId:''});if(f.geometry.type==='Point'){const map=mapRef.current?.getMap();if(map){focusBewegingRef.current=true;map.easeTo({center:(f.geometry as Point).coordinates as [number,number],zoom:Math.max(map.getZoom(),17),offset:[0,110],duration:420});}}}},[]);\n const geselecteerdPunt=useMemo(()=>{if(!geselecteerd)return null;const f=punten.features.find(x=>x.properties.id===geselecteerd.id);return f?.geometry.coordinates as [number,number]|undefined;},[punten.features,geselecteerd]); const geselecteerdRij=useMemo(()=>rows.find(row=>row.identificatie===geselecteerd?.id)??null,[rows,geselecteerd]); const kandidaatGeselecteerd=geselecteerd?geselecteerdeIds.has(geselecteerd.id):false;\n const kopieerBagId=async()=>{if(!geselecteerd)return;try{await navigator.clipboard.writeText(geselecteerd.id);toast.success('BAG-ID gekopieerd.');}catch{toast.error('BAG-ID kopiëren mislukt.');}};\n return <div className="border-b p-4"><div className="mb-3 flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><MapPinned className="h-4 w-4"/><h3 className="text-sm font-medium">Kaart</h3>{heeftGezocht&&<Badge variant="outline">{totaalMatches.toLocaleString('nl-NL')} panden</Badge>}{serverClusterMode&&<Badge variant="secondary">Clusters</Badge>}{afgekapt&&<Badge variant="secondary">Max. {KAART_LIMIET.toLocaleString('nl-NL')}</Badge>}</div><p className="mt-1 text-xs text-muted-foreground">Uitgezoomd zie je clusters over alle matches. Vanaf zoomniveau 17 verschijnen BAG-pandcontouren. Verplaats of zoom en kies daarna ‘Zoek in dit gebied’.</p></div><Button onClick={()=>void zoekInKaartgebied()} disabled={laden}>{laden?<Loader2 className="mr-2 h-4 w-4 animate-spin"/>:kaartVerouderd?<RefreshCw className="mr-2 h-4 w-4"/>:<MapPinned className="mr-2 h-4 w-4"/>}Zoek in dit gebied</Button></div>{afgekapt&&<div className="mb-3 rounded-md border bg-muted/20 px-3 py-2 text-xs text-muted-foreground">Dit detailgebied bevat meer dan {KAART_LIMIET.toLocaleString('nl-NL')} individuele panden. Zoom verder in voor een complete selectie.</div>}<div className="relative h-[430px] overflow-hidden rounded-lg border bg-muted/10 md:h-[520px]"><Map ref={mapRef} mapLib={maplibregl} mapStyle={STYLE as never} initialViewState={initiëleSessie?.viewState??AMSTERDAM_VIEWPORT} onMoveEnd={()=>{const map=mapRef.current?.getMap();if(map){const c=map.getCenter();bewaarKaartSessie({scopeCode,filterKey,rows,heeftGezocht,viewState:{longitude:c.lng,latitude:c.lat,zoom:map.getZoom()}});}if(focusBewegingRef.current){focusBewegingRef.current=false;return;}if(heeftGezocht)setKaartVerouderd(true);}} onClick={onClickKaart} interactiveLayerIds={['bag-server-clusters','bag-v3-panden']} cursor="pointer"><NavigationControl position="bottom-right" showCompass={false}/><Source id="bag-kaart-v3" type="geojson" data={punten}><Layer {...CLUSTER_LAYER}/><Layer {...CLUSTER_TELLER}/><Layer {...PAND_LAYER}/></Source><Source id="bag-pandcontouren" type="geojson" data={contouren}><Layer {...CONTOUR_FILL}/><Layer {...CONTOUR_LINE}/></Source>{geselecteerd&&geselecteerdPunt&&<Popup longitude={geselecteerdPunt[0]} latitude={geselecteerdPunt[1]} closeOnClick={false} onClose={()=>setGeselecteerd(null)} maxWidth="320px" anchor="bottom" offset={12}><div className="min-w-[240px] p-1 text-sm text-foreground"><div className="flex items-start justify-between gap-2"><div><p className="font-semibold leading-tight">{geselecteerd.adres}</p><p className="mt-1 text-xs text-muted-foreground">{[geselecteerd.postcode,geselecteerd.plaats].filter(Boolean).join(' ')}</p></div>{kandidaatGeselecteerd&&<Badge variant="secondary" className="shrink-0 text-[10px]">Geselecteerd</Badge>}</div><div className="mt-3 grid grid-cols-2 gap-x-3 gap-y-1 text-xs text-muted-foreground">{geselecteerd.status&&<p className="col-span-2">{geselecteerd.status}</p>}{geselecteerd.bouwjaar!==null&&<p>Bouwjaar {geselecteerd.bouwjaar}</p>}{geselecteerd.gbo!==null&&<p>{formatGetal(geselecteerd.gbo,' m² GBO')}</p>}{geselecteerd.vboAantal!==null&&<p>{formatGetal(geselecteerd.vboAantal,' VBO')}</p>}{geselecteerd.wijk&&<p className="col-span-2">Wijk: {geselecteerd.wijk}</p>}{geselecteerd.buurt&&<p className="col-span-2">Buurt: {geselecteerd.buurt}</p>}</div><div className="mt-3 grid gap-2">{geselecteerdRij&&onKandidaatToggle&&<Button size="sm" variant={kandidaatGeselecteerd?'secondary':'default'} className="w-full justify-center text-xs" onClick={()=>onKandidaatToggle(kaartRijNaarVerkennerPand(geselecteerdRij))}><Check className="mr-1.5 h-3.5 w-3.5"/>{kandidaatGeselecteerd?'Deselecteer kandidaat':'Selecteer kandidaat'}</Button>}<div className="grid grid-cols-2 gap-2"><Button asChild size="sm" variant="outline" className="text-xs"><a href={bouwGoogleMapsAdresUrl({adres:geselecteerd.adres,postcode:geselecteerd.postcode||null,plaats:geselecteerd.plaats||null})} target="_blank" rel="noreferrer"><ExternalLink className="mr-1.5 h-3.5 w-3.5"/>Google Maps</a></Button><Button size="sm" variant="outline" className="text-xs" onClick={()=>void kopieerBagId()}><Copy className="mr-1.5 h-3.5 w-3.5"/>BAG-ID</Button></div></div><p className="mt-3 truncate font-mono-data text-[10px] text-muted-foreground">BAG-pand {geselecteerd.id}</p></div></Popup>}</Map>{!heeftGezocht&&<div className="pointer-events-none absolute inset-x-3 top-3 flex justify-center"><div className="rounded-md border bg-background/95 px-3 py-2 text-xs shadow-sm">Kies ‘Zoek in dit gebied’ om de kaartresultaten te laden.</div></div>}</div></div>;\n}\n`);
